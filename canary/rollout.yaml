strategy:
  canary:
    stableService: demo-stable
    canaryService: demo-canary

    trafficRouting:
      nginx:
        stableIngress: demo-ingress

    steps:
      - setWeight: 20
      - pause: {}

      - setWeight: 50
      - pause: {}

      - setWeight: 100
